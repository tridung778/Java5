// function updatePrice(input, price) {
//     var quantity = input.value;
//     var index = input.parentNode.parentNode.rowIndex - 1;
//     var totalPrice = quantity * price;
//     document.getElementById('total_' + index).innerHTML = totalPrice;
// }
